# 🚀 Инструкция по запуску сайта

## Быстрый запуск

### Вариант 1: Двойной клик (самый простой)
1. Найдите файл `start_site.command` в папке `/Users/lolparty/Documents/site/`
2. Дважды кликните по нему
3. Браузер откроется автоматически с сайтом
4. Для остановки: нажмите `Ctrl+C` в терминале или закройте окно терминала

### Вариант 2: Через терминал
```bash
cd /Users/lolparty/Documents/site
./start_site.command
```

---

## 🔄 Как сделать сайт всегда запущенным

### Метод 1: Автозапуск при старте системы (launchd)

Создайте файл автозапуска:

```bash
mkdir -p ~/Library/LaunchAgents
cat > ~/Library/LaunchAgents/com.stalo.website.plist << 'EOF'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.stalo.website</string>
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/python3</string>
        <string>-m</string>
        <string>http.server</string>
        <string>8000</string>
    </array>
    <key>WorkingDirectory</key>
    <string>/Users/lolparty/Documents/site</string>
    <key>RunAtLoad</key>
    <true/>
    <key>KeepAlive</key>
    <true/>
    <key>StandardErrorPath</key>
    <string>/tmp/stalo-website-error.log</string>
    <key>StandardOutPath</key>
    <string>/tmp/stalo-website-output.log</string>
</dict>
</plist>
EOF
```

Загрузите сервис:
```bash
launchctl load ~/Library/LaunchAgents/com.stalo.website.plist
```

**Команды управления:**
- Остановить: `launchctl unload ~/Library/LaunchAgents/com.stalo.website.plist`
- Запустить снова: `launchctl load ~/Library/LaunchAgents/com.stalo.website.plist`
- Удалить из автозапуска: `rm ~/Library/LaunchAgents/com.stalo.website.plist`

---

### Метод 2: Screen (для продвинутых пользователей)

```bash
# Установить screen если нет
brew install screen

# Запустить в фоновом режиме
screen -dmS stalo-site bash -c 'cd /Users/lolparty/Documents/site && python3 -m http.server 8000'

# Посмотреть логи
screen -r stalo-site

# Выйти из просмотра (не останавливая): Ctrl+A, затем D
```

**Команды управления:**
- Остановить: `screen -X -S stalo-site quit`
- Проверить статус: `screen -ls`

---

### Метод 3: Простой фоновый запуск (временный)

```bash
cd /Users/lolparty/Documents/site
nohup python3 -m http.server 8000 > /tmp/stalo-site.log 2>&1 &
```

**Команды управления:**
- Найти процесс: `ps aux | grep "http.server 8000"`
- Остановить: `pkill -f "http.server 8000"`

---

## 📍 Доступ к сайту

После запуска, сайт доступен по адресу:
- **Локально**: http://localhost:8000
- **С других устройств в сети**: http://[IP-АДРЕС-ВАШЕГО-MAC]:8000

Чтобы узнать ваш IP адрес:
```bash
ipconfig getifaddr en0
```

---

## ⚠️ Важные замечания

1. **Порт 8000** - если занят, выберите другой (например, 8080, 3000)
2. **Firewall** - может потребоваться разрешить входящие соединения в настройках
3. **Безопасность** - для публичного доступа используйте nginx или Apache
4. **Производительность** - для продакшена используйте профессиональный веб-сервер

---

## 🎨 Структура проекта

```
/Users/lolparty/Documents/site/
├── index.html          # Главная страница
├── style.css           # Стили
├── script.js           # JavaScript
├── start_site.command  # Скрипт запуска
└── assets/             # Изображения
    ├── logo.png
    ├── img1.jpg
    ├── img2.jpg
    ├── img3.jpg
    ├── img4.jpg
    └── img5.jpg
```

---

## 🔧 Редактирование контента

Чтобы изменить содержимое сайта, отредактируйте файл `index.html` в любом текстовом редакторе. После сохранения просто обновите страницу в браузере (F5 или Cmd+R).

---

## 📱 Доступ с телефона/планшета

1. Убедитесь, что ваш Mac и мобильное устройство в одной WiFi сети
2. Узнайте IP адрес Mac: `ipconfig getifaddr en0`
3. На мобильном устройстве откройте: `http://[IP-адрес]:8000`

Например: `http://192.168.1.100:8000`

---

## ❓ Решение проблем

**Сайт не открывается:**
- Проверьте, запущен ли сервер: `ps aux | grep http.server`
- Проверьте, свободен ли порт: `lsof -i :8000`

**Изображения не загружаются:**
- Убедитесь, что все файлы на месте: `ls -la assets/`

**Сайт работает медленно:**
- Используйте профессиональный веб-сервер (nginx, Apache)
- Оптимизируйте изображения

---

Создано для инвесторов проекта "Стало" 🚁
